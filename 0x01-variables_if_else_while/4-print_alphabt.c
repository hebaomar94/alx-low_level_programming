#include <stdio.h>
/**
 * main Enry level
 * *
 * return 0
 */
int main(void)
{
	char alphabet;

	for (alphabet='a' ; alphabet <=z ; alphabet++)
	{
		if (alphabet == 'q')
			continue;
		if (alphabet == 'e')
			continue;
		putchar(alphabet);
	}

	putchar("\n")

		return (0)

